# eslint-plugin-sonarjs@0.5 peers eslint<=6 while the project uses 7. npm 6 ignored this in 2021;
# npm 7+ refuses. Lint tooling only, so it cannot affect what the API does at runtime.
legacy-peer-deps=true
